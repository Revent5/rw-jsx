# rw-jsx

```js
[ref, computed, effect, watch, reactive, nextTick];
[router];
[store];
[style];
[provide - inject];
[
	onMounted, 
	onBeforeUpdate, 
	onBeforeMount, 
	onUpdated, 
	onBeforeUnmount, 
	onUnmounted
];
```
